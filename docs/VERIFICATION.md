# 验证记录

## 2026-09-09：相机、材质、批处理与可移植发布

- 相机数学验证通过：角色跟随、高差、父级变换、轨道旋转、传送、遮挡调整。默认注视点不再被远处选中目标拉偏。
- 三职业循环验证通过，覆盖时间推进、技能队列、范围技能、治疗、移动及状态过期。
- 完整两地图重建记录：`20260909T105118Z-43c2c6`，材料错误均为 0。
- 独立源矩阵审计覆盖 9,476 个实例，最大绝对误差小于 `9e-16`；已保留源 UV0–UV3 与顶点颜色。
- 单地图 `--no-publish` 和故意失败的验证步骤均未修改当前指针。
- 最终 `bundled/` 快照通过模型、贴图存在性与原始碰撞验证，共 2,271 文件、400,144,768 字节。
- 离线动画原型重新验证结果记录在 `ANIMATION-DECODE.md`；不表示已完成游戏角色蒙皮整合。

- 成品独立目录启动通过：不含 node_modules，/ff14-web 自动 308 到尾斜杠路径，所有资源请求保持前缀，无 HTTP 错误或浏览器异常。
- 成品两地图往返、相机、目标、返回木人及三职业基础技能通过；动画 proof 再验证为 101 节点、276 轨道，1 秒时 85 节点变化。

浏览器证据见 [BROWSER-VERIFICATION.md](BROWSER-VERIFICATION.md)。地图数值证据见 [MAP-VERIFICATION.md](MAP-VERIFICATION.md)。部署启动方法见 [DEPLOYMENT.md](DEPLOYMENT.md)。

## 2026-09-09：真实 WeGame 客户端地图

- 已从 `ffxivgame.ver = 2026.09.01.0000.0000` 的本机客户端读取两处地图，源文件只读。
- 新街首轮导出 1,735 个文件，海都首轮导出 2,100 个文件；模型转换错误 0。
- 组装验证：新街 699 个模型/2,452 个实例/98,518 个原始碰撞三角形；海都 771 个模型/7,024 个实例/227,082 个原始碰撞三角形。
- `verify:extracted` 检查全部 GLB、法线/UV/索引/边界、矩阵与贴图存在性，以及真实碰撞面的出生点和移动。
- 浏览器中两张地图加载成功，均使用透视相机；角色站在原始碰撞面，闪灼造成 330 威力，未出现脚本异常。
- 27 个客户端地标落地检查全部通过，移动端视口无横向溢出，实际技能图标加载正常。
- 传统右键 11 项检查通过；位移受原始栏杆碰撞限制时会提前停止，回退返回记录位置。

本轮原始证据：`work/verify-client-world.json`、`work/client-landmarks.json`、`work/controls-check.json`；资源清单位于忽略的本地目录 `public/extracted/`。材质/光照/动态场景限制见 [CLIENT-MAPS.md](CLIENT-MAPS.md)。

## 2026-09-09：三维地形、美术、图标和传统操作更新

- `npm run verify`：三职业原有循环验证通过，本轮未修改战斗规则。
- `npm run verify:terrain`：新街 12 条道路、9 个地标，15 个可达目标；海都 18 条道路、12 个地标，17 个可达目标。主要网络连通率分别为 100% 和 99.8%。
- 3D 城市浏览器检查：27 项通过，包括保留透视相机、自由环绕、两处地图、各地标可达/落地高度、实际图标加载和高台地面效果。
- 传统操作浏览器检查：11 项通过，包括世界/HUD 右键菜单屏蔽、右键指针锁定、视角转动/角色同步、松键释放、左键独立观察、失焦清理和模式保存。
- 原有交互回归：14 项通过，包括读条、传送、身位资源、位移返回、治疗、NPC 和怪物注册。
- Windows PowerShell 5.1 启动：带 BOM 编码解析、从其他目录启动、重复启动复用、占用端口顺延和 `Start.cmd` 双击入口均验证通过。错误信息会保留并写日志。

当前仍为第三人称三维 FF14 式练习客户端；早期 JRPG 仅指美术方向。地形是原图人工描绘，不是客户端网格导出。图标文件信息来自灰机 API，图片因灰机 CDN 限制实际从 XIVAPI 同编号资源获取，完整对应见 `public/icons/manifest.json`。

本轮证据：`work/city-check.json`、`work/controls-check.json`、`work/interaction-check.json`。下面保留首版验收范围供参考。

验证日期：2026-09-09。项目使用 Three.js 0.180.0、Vite 6.4.3；生产构建使用本机附带的 Node.js 24.19.0。

## 结果

| 检查 | 结果 |
| --- | --- |
| `npm run verify` | 通过 |
| 生产构建 | 通过；Three.js 核心块约 503 KB，存在 Vite 默认的体积提示，无构建错误 |
| 构建后浏览器交互 | 14 项通过，无脚本异常 |
| 实际 WebGL 像素、动画与布局 | 23 项通过 |
| 绘灵法师地面范围与重置 | 通过 |
| 不同工作目录启动静态预览 | 通过 |

职业验证覆盖三个插件的资源生成/消耗、技能阶段转换、公共/独立复唱与充能、咏唱中断、目标失效、输入队列、范围衰减、身位、持续伤害、治疗/护盾、位移事件和状态到期。

浏览器实际操作覆盖移动中断、白魔法师触发技能、治疗压力与恢复、技能书、传送取消、钐镰客三连、地狱门返回、NPC 射线选择、动态怪物注册与选择、手机布局。

两个场景分别采样 81 个 WebGL 像素点，均有超过 60 种有效颜色；场景时钟持续推进。质量切换后阴影贴图实际在 768 与 1536 间切换。

布局检查使用六种视口，分别对三个职业验证技能栏、量谱和顶栏：1920×1080、1440×900、1024×768、390×844、360×740、844×390。

星空范围内实测 1.5 秒魔法缩短到 1.125 秒；移出范围后恢复 1.5 秒且不消耗超绝想象层数。重置后场地和日志清空；海都水面禁止通行，可见甲板仍可行走。

## 本地证据

- `scripts/verify.mjs`：可重复执行的职业规则验证。
- `work/interaction-check.json`：构建后实际交互记录。
- `work/render-check.json`：构建后像素、质量和多视口记录。
- `work/field-check.json`：场地效果和导航检查。

这些检查证明本训练器的上述行为可运行，不代表已与真实 7.0 客户端进行逐帧一致性验证。场景为原创程序化模型；战斗近似与未确认项明确列于 [RULES.md](RULES.md)。
