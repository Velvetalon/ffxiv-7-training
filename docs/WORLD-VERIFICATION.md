# 完整世界验证记录

验证日期：2026-09-09。资源版本：`20260909T124255Z-e3ea44`。客户端资产版本：`2026.09.01.0000.0000`；职业规则与区域范围基于 7.0。

## 转换范围与数据

- 65 个区域：47 个户外地图和 18 个城市、公共据点。
- 51,794 个模型资源，523,489 个摆放实例，54,850,891 个原始碰撞三角形。
- 全部区域的模型、材质引用、实际索引范围、顶点/法线有限值、缓冲区边界、碰撞及练习落地点检查通过。
- 独立原始布局与 GLB 审计 65/65 通过；原始 GLB 与组装副本逐字节一致。最大实例矩阵差异为 `0.0000763278` 个坐标单位。
- 66 个源 UV/颜色非有限分量单独记录，运行时仅替换这些无效分量，源文件和正常分量保持不变。

逐区域汇总与原始审计数值分别见 `WORLD-CONVERSION-SUMMARY.json`、`WORLD-SOURCE-AUDIT.json`。

## 区域连接

65 份原始 `planmap.lgb` 解析出 145 条清单内有效区域连接。134 条通过目标 `PopRange` 与反向 `ExitRange` 的实例 ID 互换严格配对；另 11 条使用源记录明确指定的目标入口。

全部 145 条连接通过目标存在性、来源字段一致性、入口位置投影范围、实际碰撞落地、配对或显式到达坐标检查。原始的触发体和入口坐标保留在来源记录中。

12 个区域没有普通 `ExitRange` 连接，仍可通过完整传送目录进入；未伪造原版不存在的直接步行出口。38 条指向副本等清单外区域的记录及 2 条没有目标实例 ID 的记录已明确列为排除项。

## 无损发布包

- 31,924 张纹理逐文件校验，使用无损 WebP 时解码 RGBA 字节必须完全相同；23,309 张采用 WebP，其余保持原 PNG。
- 纹理总字节从 12,792,807,815 降至 11,424,474,439。
- 碰撞数据从 1,974,632,076 字节压缩到 522,581,991 字节；逐文件解压后校验原始长度与 SHA-256。
- 原始模型副本、地图图片、输出纹理与碰撞均有哈希记录。断点复用会核对实际文件。

## 浏览器完整验收

最终 `site/` 在独立静态服务器的 `/ff14-web/` 子路径运行。入口 HTML SHA-256：

`b148a02865cebb149f9f7195f53e0810c6e65ae0e4f63ac8a7e1bf60187a1288`

65/65 个区域均完成真实资源加载、模型数量匹配、gzip 碰撞加载、纹理请求、角色落地和移动、相机中心、小地图比例/偏移、区域内全部出口可达检查。没有浏览器异常、渲染 GL 错误或失败资源请求。

四类代表性实际 F 键跨区检查均通过，抵达指定入口的距离误差为 0：

- 城市：海都上层甲板 → 拉诺西亚低地。
- 户外：中拉诺西亚 → 海都下层甲板。
- 资料片：库尔札斯西部高地的源数据对应出口。
- 无严格反向 ID 的入口：北部林区 → 中央林区，按源记录指定的显式坐标到达。

精确连接 ID、目标与浏览器结果保存在 `WORLD-BROWSER-VERIFICATION.json`。战斗中禁止跨区、失败后保留原地图和重试行为另有独立试点验证。三个职业循环与相机数学回归通过。

## 表现边界

地图按区域保留本地坐标并通过连接点切换，不是将所有区域网格同时拼成单一无缝世界。原引擎完整着色器、水面、环境光、动态事件和剧情状态仍有近似。角色、木人及 NPC 仍为练习器模型；地图验证不表示已实现原版角色蒙皮动画。

服务器切换、最终 Git SHA、上传文件哈希与原站健康状态由具体发布记录证明，不能用本文件的本地验收代替部署证据。

## 2026-09-10：资产运行时生命周期修复发布前复核

- 兼容构建 `work/final-world-full-v3.json` 的既有 65/65 完整验收仍为通过基线；本轮未重复全量或性能对比运行。
- 对照测试以 `223c5b478b` 的旧 `AssetRuntime.load` 重现了零预算并发父资源场景中的依赖资源过早驱逐；当前实现的运行时 fixture 通过。修复使 inflight 引用保留至所有客户端取得资源，并在依赖阶段保留所需数据。
- 本轮自动 F 键代表边全部通过：`d2t1:exit:5865672 -> d2f2`、`f1f1:exit:1317623 -> gridania`、`a2f1:exit:5866156 -> r2t2`、`a2f1:exit:5866153 -> r2t2`。每条检查完成源/目标加载、入口定位、碰撞落地及页面诊断。
- 生产构建的 `active.json` SHA-256 为 `6ebbd33a7e738a2c0aa895c64c7bdb17c28bdad3bf74afabdfc9a31b388a8623`，含 65 个场景并使用 ticket 端点；正式服务器切换与公网回查见本次发布记录。
